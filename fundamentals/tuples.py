tuple = (1,2,3,4,5)
# tuple are immutable in python

# Accessing elements of a tuple
print(tuple[0])
print(tuple[4])

# Changing the value of a tuple
tuple[4] = 6
print(tuple) # this will give an error as tuple are immutable
